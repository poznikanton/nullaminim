// Assign a function to the functionContext object with the key "name"
functionContext[name] = fn;

// If there is an astVisitor, register it as well
if (astVisitor) {
  // Register the astVisitor
}
